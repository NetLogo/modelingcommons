<% @title = "Group search results" %>

<ul>
    <% @groups.each do |group| %>
        <li>
          <%= group.name %>
            <% if not group.administrators.empty? %>
                (Group owner: <%= person_link(group.administrators.first.person) %>)
            <% end %>
        </li>
    <% end %>
</ul>
